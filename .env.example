# Pi 5 Next.js Starter - ต้นแบบ Environment Variables
# คัดลอกเป็น .env และแก้ค่าตาม environment

# ชื่อโปรเจกต์ (ใช้กับ Docker container name)
PROJECT_NAME=pi5-nextjs-starter

# พอร์ตที่ expose บน Host (ตรงกับ HOST_PORT ใน Jenkinsfile)
HOST_PORT=3001

# ตัวแปรเพิ่มเติมสำหรับแอป (ตัวอย่าง)
# NEXT_PUBLIC_API_URL=https://api.example.com
# DATABASE_URL=postgresql://...
