# Pi 5 Next.js + NextAuth + MongoDB - ต้นแบบ Environment Variables
# คัดลอกเป็น .env และแก้ค่าตาม environment

# --- MongoDB (Singleton connection) ---
# ชี้ไปที่ shared-mongodb หรือ instance ของคุณ
MONGODB_URI=mongodb://admin_mongo:dd93b767368cdccdb97fd3fe55163049@localhost:27017/?authSource=admin

# --- NextAuth + Google OAuth ---
# จาก Google Cloud Console > APIs & Services > Credentials
# ต้องเพิ่ม Authorized redirect URIs ใน OAuth 2.0 Client ID:
#   - Dev:  http://localhost:4242/api/auth/callback/google
#   - Prod: https://<โดเมนของคุณ>/api/auth/callback/google  (เช่น https://fin.kim.wtf/api/auth/callback/google)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# URL ของแอป (dev: localhost, prod: domain จริง)
NEXTAUTH_URL=http://localhost:4242
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Redirect URIs (ต้องตรงกับที่ลงทะเบียนใน Google Console)
# REDIRECT_URI = OAuth callback ที่ Google เรียกกลับ (ใช้ฝั่ง server)
# NEXT_PUBLIC_REDIRECT_URI = หน้าที่จะพา user ไปหลัง login (ใช้ฝั่ง client + redirect callback)
# ค่าต้องตรงกันระหว่าง local กับ web ตาม environment
REDIRECT_URI=http://localhost:4242/api/auth/callback/google
NEXT_PUBLIC_REDIRECT_URI=http://localhost:4242

# --- โปรเจกต์ (ใช้กับ Docker container name) ---
PROJECT_NAME=fin-kim-wtf
HOST_PORT=4242
